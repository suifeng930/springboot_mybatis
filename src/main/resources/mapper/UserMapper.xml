<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mhc.springboot_mybatis.dao.UserDao">

    <select id="findUserByNameWithXML" parameterType="String" resultType="com.mhc.springboot_mybatis.entity.User">
        SELECT * FROM user WHERE name = #{name}
    </select>
</mapper>